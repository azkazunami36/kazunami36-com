import { sumPageInit } from "../src/js/sumPageInit.js";
addEventListener("load", async () => {
    sumPageInit();
    const novelNames = [
        {
            name: "男の僕と女の僕。記憶は同じ！？"
        }
    ];
});
